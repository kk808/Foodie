import type { Config } from "tailwindcss";
// Placeholder theme extension — will be regenerated from `@foodie/tokens`
// output once the Style Dictionary pipeline (build plan §2) is wired up.
// import { theme } from "@foodie/tokens/tailwind-theme";

const config: Partial<Config> = {
  theme: {
    extend: {
      // theme,
    },
  },
};

export default config;
