// Superseded by eslint.config.js (couldn't delete this file from the mounted
// project folder in the Cowork session that did this migration — FUSE mount
// limitation, see DONE.md). Kept in sync so it's harmless either way.
import preset from "../../packages/config/eslint-preset.js";
export default preset;
